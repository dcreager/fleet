prefix=@prefix@
exec_prefix=${prefix}
libdir=${exec_prefix}/lib
includedir=${prefix}/include
sharedir=${prefix}/share

Name: Fleet
Description: Fast and lightweight user-level threading and channels
Version: @VERSION@
URL: https://github.com/redjack/fleet/
Libs: -L${libdir} -lfleet
Cflags: -I${includedir}
